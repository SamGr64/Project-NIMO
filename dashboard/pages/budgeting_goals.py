def render(ctx) -> None:
    import streamlit as st
    st.title("Budgeting & Goals")
    st.info("Phase 8 scaffold: default budgets, custom budget lines and probabilistic goals will use the forecasting services added after behaviour inference.")
