# Database migrations

Version 0.5.0 creates the initial schema with SQLAlchemy metadata. Before the first schema-breaking release, introduce explicit numbered migrations here and record the applied schema version in each user database.
